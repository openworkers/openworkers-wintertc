{
  "name": "openworkers-wintertc",
  "version": "0.1.0",
  "private": true,
  "description": "Bun corpus for the shared WinterTC surface: the inner loop, run without an engine embedding",
  "type": "module",
  "scripts": {
    "test": "bun test"
  }
}
